{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "buildCommand": "cd e_commerce_backend && pip install -r requirements.txt"
  },
  "deploy": {
    "startCommand": "cd e_commerce_backend && alembic upgrade head && uvicorn app.main:app --host 0.0.0.0 --port $PORT"
  }
}
